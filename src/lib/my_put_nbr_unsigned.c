/*
** EPITECH PROJECT, 2024
** my_put_nbr_unsigned.c
** File description:
** Display an unsigned number
*/
#include "my.h"
#include <unistd.h>
#include <stdlib.h>
/*
char *my_put_nbr_unsigned(unsigned int nb)
{
    char *str;
    int count = 0;

    if (nb > 9) {
    my_put_nbr_unsigned(nb / 10);
    str[0] = my_putchar(nb % 10 + 48);
    count++;
    }
    str = malloc(sizeof(char) * count);
    if (nb <= 9 && nb >= 0)
        str = my_strcat(str, *my_putchar(nb + 48));
    my_putstr(str);
    return str;
}
*/
